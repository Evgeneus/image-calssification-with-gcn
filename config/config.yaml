# train options
seed: 2020 # sacred handles automatic seeding when passed in the config
batch_size: 4
workers: 0
epochs: 2
dataset: "CIFAR10"

# loss options
lr: 1.0e-3
momentum: 0.9
weight_decay: 1.0e-6

# data folder
root: "../data"
